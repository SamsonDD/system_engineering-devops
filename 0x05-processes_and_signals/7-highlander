#!/usr/bin/env bash
# Infinite loop that prints message and handles SIGTERM by printing invincible message and OK

trap 'echo OK' SIGTERM

while true; do
  echo "To infinity and beyond"
  sleep 2
done

